// Web stub: flutter_gpu is unavailable on web (dart:ffi / dart:nativewrappers
// are native-only). Provides minimal replacements so the package compiles for
// the web platform.

class Texture {}

Future<Texture?> gpuTextureFromImage(dynamic image) async => null;
